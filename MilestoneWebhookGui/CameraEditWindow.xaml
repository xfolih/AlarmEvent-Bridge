<Window x:Class="MilestoneWebhookGui.CameraEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lägg till / Redigera kamera"
        Height="650" Width="650"
        MinHeight="650" MinWidth="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Icon="pack://application:,,,/icon.ico">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Kamera:" Margin="0,0,0,4" FontWeight="SemiBold"/>
        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" Margin="0,0,0,4" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" FontSize="12" VerticalAlignment="Center" Margin="6,0,4,0" Foreground="Gray"/>
                <TextBox x:Name="TbSearchCamera" Grid.Column="1" BorderThickness="0" 
                         TextChanged="TbSearchCamera_TextChanged" 
                         Padding="0,4"
                         ToolTip="Sök efter kamera..."/>
            </Grid>
        </Border>
        <ComboBox x:Name="CbCamera" Grid.Row="2" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,12" MinHeight="26"/>
        
        <TextBlock Grid.Row="3" Text="Händelsetyp (Analyshändelse):" Margin="0,0,0,4" FontWeight="SemiBold"/>
        <Border Grid.Row="4" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" Margin="0,0,0,4" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" FontSize="12" VerticalAlignment="Center" Margin="6,0,4,0" Foreground="Gray"/>
                <TextBox x:Name="TbSearchEventType" Grid.Column="1" BorderThickness="0"
                         TextChanged="TbSearchEventType_TextChanged"
                         Padding="0,4"
                         ToolTip="Sök efter analyshändelse..."/>
            </Grid>
        </Border>
        <ComboBox x:Name="CbEventType" Grid.Row="5" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,12" MinHeight="26"/>
        
        <TextBlock Grid.Row="6" Text="Larm tillkopplat:" Margin="0,0,0,8" FontWeight="SemiBold"/>
        
        <StackPanel Grid.Row="7" Orientation="Horizontal" Margin="0,0,0,8">
            <RadioButton x:Name="RbIo" Content="Följa ingång/utgång" IsChecked="True" Checked="RbIo_Checked" Margin="0,0,16,0"/>
            <RadioButton x:Name="RbUserDefined" Content="Användardefinierade händelser" Checked="RbUserDefined_Checked"/>
        </StackPanel>
        
        <!-- IO-val (visas när RbIo är vald) -->
        <TextBlock x:Name="TbIoLabel" Grid.Row="8" Text="Ingång/utgång:" Margin="0,0,0,4" FontWeight="SemiBold"/>
        <Border x:Name="BorderIo" Grid.Row="9" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" Margin="0,0,0,4" Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="🔍" FontSize="12" VerticalAlignment="Center" Margin="6,0,4,0" Foreground="Gray"/>
                <TextBox x:Name="TbSearchIo" Grid.Column="1" BorderThickness="0"
                         TextChanged="TbSearchIo_TextChanged"
                         Padding="0,4"
                         ToolTip="Sök efter ingång eller utgång..."/>
            </Grid>
        </Border>
        <ComboBox x:Name="CbIo" Grid.Row="10" DisplayMemberPath="Display" SelectedValuePath="Id" Margin="0,0,0,12" MinHeight="26"/>
        
        <!-- Användardefinierade händelser (visas när RbUserDefined är vald) -->
        <TextBlock x:Name="TbUserDefinedLabel" Grid.Row="8" Text="Användardefinierade händelser för larmstatus:" Margin="0,0,0,4" FontWeight="SemiBold" Visibility="Collapsed"/>
        <Grid x:Name="GridUserDefined" Grid.Row="9" Margin="0,0,0,12" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Larm aktiv:" VerticalAlignment="Center" Margin="0,0,6,4" MinWidth="90"/>
            <Border Grid.Row="0" Grid.Column="1" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" Margin="0,0,12,4" Background="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🔍" FontSize="10" VerticalAlignment="Center" Margin="4,0,2,0" Foreground="Gray"/>
                    <TextBox x:Name="TbSearchAlarmActive" Grid.Column="1" BorderThickness="0"
                             TextChanged="TbSearchAlarmActive_TextChanged"
                             Padding="0,2"
                             FontSize="10"
                             ToolTip="Sök efter händelsetyp..."/>
                </Grid>
            </Border>
            <TextBlock Grid.Row="0" Grid.Column="2" Text="Larm inaktiv:" VerticalAlignment="Center" Margin="0,0,6,4" MinWidth="90"/>
            <Border Grid.Row="0" Grid.Column="3" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="3" Margin="0,0,0,4" Background="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🔍" FontSize="10" VerticalAlignment="Center" Margin="4,0,2,0" Foreground="Gray"/>
                    <TextBox x:Name="TbSearchAlarmInactive" Grid.Column="1" BorderThickness="0"
                             TextChanged="TbSearchAlarmInactive_TextChanged"
                             Padding="0,2"
                             FontSize="10"
                             ToolTip="Sök efter händelsetyp..."/>
                </Grid>
            </Border>
            <ComboBox x:Name="CbAlarmActive" Grid.Row="1" Grid.Column="1" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,12,0" MinHeight="24"/>
            <ComboBox x:Name="CbAlarmInactive" Grid.Row="1" Grid.Column="3" DisplayMemberPath="Name" SelectedValuePath="Id" MinHeight="24"/>
        </Grid>
        
        <TextBlock Grid.Row="11" Text="Webhook-URL:" Margin="0,0,0,4" FontWeight="SemiBold"/>
        <TextBox x:Name="TbWebhookUrl" Grid.Row="12" Margin="0,0,0,12" MinHeight="26" ToolTip="T.ex. http://127.0.0.1:8765/46001/ID001/01/MILESTONE"/>
        
        <StackPanel Grid.Row="17" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button IsCancel="True" Click="BtnCancel_Click" Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="❌" FontSize="14" Margin="0,0,4,0"/>
                    <TextBlock Text="Avbryt"/>
                </StackPanel>
            </Button>
            <Button IsDefault="True" Click="BtnSave_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✓" FontSize="14" Margin="0,0,4,0"/>
                    <TextBlock Text="Spara"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>
